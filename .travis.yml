language: node_js
node_js:
  - 14.16.0
cache:
  npm install
  
script:
  - npm test 
  - npm run build # Generate static HTML files

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master